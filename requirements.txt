# --- PyTorch & CUDA Dependencies (Requires Special Index URL) ---
# NOTE: The CUDA 12.8 version of torch/torchvision/torchaudio must be installed 
# using the PyTorch index URL specified below, due to custom wheel building.
torch==2.8.0+cu128
torchvision==0.23.0+cu128
torchaudio==2.5.1
triton==3.4.0

# --- Celery & Redis Backend ---
# Celery requires the redis extra to use Redis as a broker/backend.
celery[redis]
redis # Python client for Redis

# --- Backend & Core Web Frameworks ---
fastapi # (Cleaned from local path)
uvicorn # (Cleaned from local path)
python-multipart # For handling file uploads (Cleaned from local path)
aiofiles # For async file I/O (Used in main.py)
orjson==3.11.3
pydantic # (Cleaned from local path)
pydantic-extra-types==2.10.6
pydantic-settings==2.11.0
httpx # (Cleaned from local path)

# --- Deep Learning & Scientific Computing ---
timm==1.0.20
opencv-python==4.10.0 # Used for GFPGAN (Cleaned from local path)
opencv-python-headless==4.10.0 # (Cleaned from local path)
numpy # (Using PyPI - version 1.26.4 found in logs)
pandas # (Using PyPI - version 2.2.2 found in logs)
scipy # (Using PyPI - version 1.16.2 found in logs)
scikit-learn # (Using PyPI - version 1.7.2 found in logs)
tqdm==4.67.1
pillow # (Using PyPI - version 10.3.0 found in logs)

# --- Other Explicitly Pinned Dependencies ---
matplotlib==3.10.6
safetensors==0.6.2
huggingface-hub==0.35.3
fsspec==2025.9.0
fastapi-cloud-cli==0.3.1
itsdangerous==2.2.0
mkl-service==2.4.0
munkres==1.1.4
PySide6==6.7.3
shiboken6==6.7.3
sentry-sdk==2.41.0
sympy==1.13.3
typer==0.19.2
typer-slim==0.19.2
ujson==5.11.0

# --- General Python (Cleaned from local paths) ---
requests
PyYAML
psutil
packaging